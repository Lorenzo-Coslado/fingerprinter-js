# Configuration pour GitHub Packages
@lorenzo-coslado:registry=https://npm.pkg.github.com/

# Token d'authentification pour GitHub Packages (utilise NODE_AUTH_TOKEN en CI/CD)
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}

# Configuration pour NPM (par défaut)
# Utilise les credentials de `npm login` pour les publications locales
